<UserControl x:Class="Redact1.Views.FileReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:Redact1.Converters"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:StatusToColorConverter x:Key="StatusToColorConverter" />
        <converters:StatusToBackgroundConverter x:Key="StatusToBackgroundConverter" />
        <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <converters:BoolToVisibilityInverseConverter x:Key="BoolToVisibilityInverseConverter" />
        <converters:CountToVisibilityInverseConverter x:Key="CountToVisibilityInverseConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <!-- Image/PDF Viewer -->
        <Grid Grid.Column="0" Background="#1A1A1A">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <Border Grid.Row="0"
                    Background="{StaticResource PrimaryBrush}"
                    Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="Close"
                            Style="{StaticResource SecondaryButton}"
                            Foreground="White"
                            BorderBrush="White"
                            Background="Transparent"
                            Click="CloseButton_Click" />

                    <TextBlock Grid.Column="1"
                               Text="{Binding File.Filename}"
                               Foreground="White"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="Draw Redaction"
                                Style="{StaticResource SecondaryButton}"
                                Foreground="White"
                                BorderBrush="White"
                                Background="Transparent"
                                Command="{Binding ToggleDrawingModeCommand}"
                                Margin="0,0,8,0" />

                        <Button Content="Preview"
                                Style="{StaticResource SecondaryButton}"
                                Foreground="White"
                                BorderBrush="White"
                                Background="Transparent"
                                Command="{Binding PreviewRedactedCommand}"
                                Margin="0,0,8,0" />

                        <Button Content="Save Redacted"
                                Style="{StaticResource PrimaryButton}"
                                Background="{StaticResource SuccessBrush}"
                                Command="{Binding SaveRedactedCommand}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Image Display -->
            <ScrollViewer Grid.Row="1"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <Grid x:Name="ImageContainer">
                    <Image x:Name="DisplayImage"
                           Source="{Binding DisplayImage}"
                           Stretch="Uniform"
                           Visibility="{Binding ShowRedacted, Converter={StaticResource BoolToVisibilityInverseConverter}}" />

                    <Image Source="{Binding RedactedImage}"
                           Stretch="Uniform"
                           Visibility="{Binding ShowRedacted, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <!-- Detection Overlays Canvas -->
                    <Canvas x:Name="OverlayCanvas"
                            MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                            MouseMove="Canvas_MouseMove"
                            MouseLeftButtonUp="Canvas_MouseLeftButtonUp" />
                </Grid>
            </ScrollViewer>

            <!-- PDF Navigation -->
            <Border Grid.Row="2"
                    Background="{StaticResource SurfaceBrush}"
                    Padding="16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Previous"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding PreviousPageCommand}"
                            Margin="0,0,16,0" />

                    <TextBlock VerticalAlignment="Center">
                        <Run Text="Page " />
                        <Run Text="{Binding CurrentPage}" />
                        <Run Text=" of " />
                        <Run Text="{Binding TotalPages}" />
                    </TextBlock>

                    <Button Content="Next"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding NextPageCommand}"
                            Margin="16,0,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Detections Panel -->
        <Border Grid.Column="1"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Detection Header -->
                <Border Grid.Row="0"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Detections"
                                   FontSize="16"
                                   FontWeight="SemiBold" />

                        <Button Grid.Column="1"
                                Content="Run Detection"
                                Style="{StaticResource PrimaryButton}"
                                Command="{Binding RunDetectionCommand}"
                                IsEnabled="{Binding IsDetecting, Converter={StaticResource InverseBoolConverter}}" />
                    </Grid>
                </Border>

                <!-- Bulk Actions -->
                <Border Grid.Row="1"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="16,12">
                    <Button Content="Approve All Pending"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding ApproveAllCommand}"
                            HorizontalAlignment="Left" />
                </Border>

                <!-- Detection List -->
                <ScrollViewer Grid.Row="2">
                    <StackPanel>
                        <!-- Auto Detections -->
                        <ItemsControl ItemsSource="{Binding Detections}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="0,0,0,1"
                                            Padding="16,12">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Row="0" Grid.Column="0">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding TextContent}"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           FontSize="12"
                                                           TextTrimming="CharacterEllipsis"
                                                           Visibility="{Binding TextContent, Converter={StaticResource NullToVisibilityConverter}}" />
                                            </StackPanel>

                                            <Border Grid.Row="0" Grid.Column="1"
                                                    Padding="8,4"
                                                    CornerRadius="4"
                                                    Background="{Binding Status, Converter={StaticResource StatusToBackgroundConverter}}">
                                                <TextBlock Text="{Binding Status}"
                                                           FontSize="12"
                                                           Foreground="{Binding Status, Converter={StaticResource StatusToColorConverter}}" />
                                            </Border>

                                            <StackPanel Grid.Row="1" Grid.ColumnSpan="2"
                                                        Orientation="Horizontal"
                                                        Margin="0,8,0,0">
                                                <Button Content="Approve"
                                                        Style="{StaticResource SecondaryButton}"
                                                        Foreground="{StaticResource SuccessBrush}"
                                                        BorderBrush="{StaticResource SuccessBrush}"
                                                        Padding="12,6"
                                                        Command="{Binding DataContext.ApproveDetectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="0,0,8,0" />

                                                <Button Content="Reject"
                                                        Style="{StaticResource SecondaryButton}"
                                                        Foreground="{StaticResource ErrorBrush}"
                                                        BorderBrush="{StaticResource ErrorBrush}"
                                                        Padding="12,6"
                                                        Command="{Binding DataContext.RejectDetectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Manual Redactions -->
                        <TextBlock Text="Manual Redactions"
                                   FontWeight="SemiBold"
                                   Margin="16,16,16,8"
                                   Visibility="{Binding ManualRedactions.Count, Converter={StaticResource CountToVisibilityInverseConverter}}" />

                        <ItemsControl ItemsSource="{Binding ManualRedactions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="0,0,0,1"
                                            Padding="16,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="Manual Redaction"
                                                       FontWeight="Medium" />

                                            <Button Grid.Column="1"
                                                    Content="Remove"
                                                    Style="{StaticResource IconButton}"
                                                    Foreground="{StaticResource ErrorBrush}"
                                                    Command="{Binding DataContext.DeleteManualRedactionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>

                <!-- Loading -->
                <Border Grid.RowSpan="3"
                        Background="#80FFFFFF"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="150" Height="4" />
                        <TextBlock Text="Processing..."
                                   Margin="0,12,0,0"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>

                <!-- Detecting -->
                <Border Grid.RowSpan="3"
                        Background="#80FFFFFF"
                        Visibility="{Binding IsDetecting, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="150" Height="4" />
                        <TextBlock Text="Detecting sensitive content..."
                                   Margin="0,12,0,0"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
