<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Redact1.Views.FileReviewView"
             Background="#18181F">

    <UserControl.Styles>
        <!-- Toolbar button styles - soft pastel on brick header -->
        <Style Selector="Button.toolbar-btn">
            <Setter Property="Background" Value="#D4B8A8" />
            <Setter Property="Foreground" Value="#3D3632" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style Selector="Button.toolbar-btn:pointerover">
            <Setter Property="Background" Value="#E5CFC2" />
        </Style>
        <Style Selector="Button.toolbar-btn:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#E5CFC2" />
            <Setter Property="Foreground" Value="#3D3632" />
        </Style>
        <Style Selector="Button.toolbar-btn:pressed /template/ ContentPresenter">
            <Setter Property="Background" Value="#C9A999" />
            <Setter Property="Foreground" Value="#3D3632" />
        </Style>

        <!-- Success button on toolbar - soft sage green -->
        <Style Selector="Button.toolbar-success">
            <Setter Property="Background" Value="#8FB8A0" />
            <Setter Property="Foreground" Value="#2D3E35" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style Selector="Button.toolbar-success:pointerover">
            <Setter Property="Background" Value="#A3C9B3" />
        </Style>
        <Style Selector="Button.toolbar-success:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#A3C9B3" />
            <Setter Property="Foreground" Value="#2D3E35" />
        </Style>
        <Style Selector="Button.toolbar-success:pressed /template/ ContentPresenter">
            <Setter Property="Background" Value="#7AA88F" />
            <Setter Property="Foreground" Value="#2D3E35" />
        </Style>
    </UserControl.Styles>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0"
                Background="{StaticResource PrimaryBrush}"
                Padding="20,14"
                CornerRadius="0,0,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Close"
                        Classes="toolbar-btn"
                        x:Name="CloseButton"
                        IsVisible="{Binding ShowDetectionPrompt}" />

                <TextBlock Grid.Column="1"
                           Text="{Binding File.Filename}"
                           Foreground="White"
                           FontWeight="SemiBold"
                           FontSize="15"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                    <Button Content="Draw Redaction"
                            Classes="toolbar-btn"
                            Command="{Binding ToggleDrawingModeCommand}"
                            IsVisible="{Binding !ShowDetectionPrompt}" />

                    <Button Content="Cancel"
                            Classes="toolbar-btn"
                            x:Name="CancelButton"
                            IsVisible="{Binding !ShowDetectionPrompt}" />

                    <Button Content="Save"
                            Classes="toolbar-success"
                            Command="{Binding SaveCommand}"
                            IsVisible="{Binding !ShowDetectionPrompt}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- PDF/Image Display -->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      Background="#18181F">
            <Grid x:Name="ImageContainer">
                <Image x:Name="DisplayImage"
                       Source="{Binding DisplayImage}"
                       Stretch="Uniform"
                       IsVisible="{Binding !ShowRedacted}" />

                <Image Source="{Binding RedactedImage}"
                       Stretch="Uniform"
                       IsVisible="{Binding ShowRedacted}" />

                <Canvas x:Name="OverlayCanvas"
                        PointerPressed="Canvas_PointerPressed"
                        PointerMoved="Canvas_PointerMoved"
                        PointerReleased="Canvas_PointerReleased" />
            </Grid>
        </ScrollViewer>

        <!-- PDF Navigation -->
        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                Padding="20,14"
                IsVisible="{Binding File.IsPdf}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16">
                <Button Content="Previous"
                        Classes="secondary"
                        Command="{Binding PreviousPageCommand}" />

                <TextBlock VerticalAlignment="Center"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}">
                    <Run Text="Page " />
                    <Run Text="{Binding CurrentPage}" FontWeight="SemiBold" />
                    <Run Text=" of " />
                    <Run Text="{Binding TotalPages}" FontWeight="SemiBold" />
                </TextBlock>

                <Button Content="Next"
                        Classes="secondary"
                        Command="{Binding NextPageCommand}" />
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3"
                Background="#C018181F"
                IsVisible="{Binding IsLoading}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="180" Height="4" Foreground="{StaticResource PrimaryBrush}" />
                <TextBlock Text="Processing..."
                           Foreground="White"
                           FontSize="14"
                           Margin="0,16,0,0"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Detecting Overlay -->
        <Border Grid.RowSpan="3"
                Background="#C018181F"
                IsVisible="{Binding IsDetecting}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="180" Height="4" Foreground="{StaticResource PrimaryBrush}" />
                <TextBlock Text="Detecting sensitive content..."
                           Foreground="White"
                           FontSize="14"
                           Margin="0,16,0,0"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Run Detection Prompt Overlay -->
        <Border Grid.Row="1"
                Background="#80000000"
                IsVisible="{Binding ShowDetectionPrompt}">
            <Button Content="Run Detection"
                    Command="{Binding RunDetectionCommand}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="#B5594C"
                    Foreground="White"
                    FontSize="18"
                    FontWeight="Bold"
                    Padding="32,16"
                    CornerRadius="12"
                    BorderThickness="0"
                    Cursor="Hand" />
        </Border>
    </Grid>
</UserControl>
